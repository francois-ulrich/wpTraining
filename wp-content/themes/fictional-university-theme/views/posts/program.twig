<div>
	<h5 class="event-summary__title headline headline--tiny">
		<a href="{{post.link}}">{{post.title}}</a>
	</h5>

	<p>{{post.preview.length(18).read_more(false)}}</p>

	<a href="{{post.title}}" class="nu gray">Learn&nbsp;more</a>

	<hr class='section-break'>
</div>
